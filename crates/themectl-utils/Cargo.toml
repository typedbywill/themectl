[package]
name = "themectl-utils"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "1"
serde_yaml = "0.9"
serde_json = "1"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
dirs = "5"

[dev-dependencies]
tempfile = "3"

