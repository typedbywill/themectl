[package]
name = "themectl-spec"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
serde_json = "1"
themectl-utils = { path = "../themectl-utils" }

[dev-dependencies]
tempfile = "3"
